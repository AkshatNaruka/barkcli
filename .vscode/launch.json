{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run barkcli Extension",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "${workspaceFolder}/vscode-extension",
        "--extensionDevelopmentPath=${workspaceFolder}/vscode-extension"
      ],
      "outFiles": [
        "${workspaceFolder}/vscode-extension/dist/**/*.js"
      ],
      "preLaunchTask": "build-extension"
    }
  ]
}
